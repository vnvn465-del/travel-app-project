#  logging (loki 가 깔려있는 네임스페이스)
apiVersion: v1
kind: ResourceQuota
metadata:
  name: logging-quota
  namespace: logging
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "800Mi"
    limits.cpu: "1000m"
    limits.memory: "2Gi"
